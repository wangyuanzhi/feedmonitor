---
applications:
  target/appassembler/:
    name: buffalo-batch
    framework:
      name: standalone
      info:
        mem: 64M
        description: Standalone Application
        exec:
    runtime: java
    command: bin/main
    url:
    mem: 256M
    instances: 1
    services:
      mysql-buffalo2:
        type: mysql
